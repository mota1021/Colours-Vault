---
tags: [Colours, 仕様, ラフ]
---
# 仕様ラフ: 光の伝播

> **使い方**: このファイルだけを編集する。境界の判定（何を仕様に書くか/演出に書くか等）は生成時にAIが行うので、ここでは自由に混在して書いてよい。
> 変更・追記したら「このラフから仕様を更新して」と依頼 → AI が `050.020.仕様` / `050.030.演出・UI` / `050.040.実装` を生成・更新する。
> ラフは出力後も保持する（次回変更のための入力）。
> **未決・相談したいこと**: 未決事項を記録するセクション。解決したら項目を削除する（解決済みコメントを残さない）。
>
> **由来**: 2026-07-10 GL7（ガラス実装クラス方針）検討中に、既存の光伝播実装（`AColoursConeLight`）が「単一ホップ・色データ非搬送・反射/屈折/透過すべて未実装」と判明。ガラス透過を成立させる前提として光伝播モデルを独立仕様として立ち上げ（`/colours-spec-interview`）。

---

## やりたいこと・目的

光がシーン内をどう進むかを、色データ（`FColoursLightValue`）を運びながらの伝播モデルとして統一的に定義する。光線は表面ヒットで色変化した継続セグメントを再発射でき（多ホップ）、始点はセグメント単位で管理し、光源・照射角度の変化にリアルタイムに追従する。

---

## ルール・挙動

- **スコープ**: 透過（乗算モデル）＋多ホップ伝播基盤（色データ搬送・継続セグメント再発射・始点管理・可逆追従）に限定する（Stage1準拠）。反射・屈折は本仕様では扱わない（将来スロット）。
- **伝播の単位**: レイ単位。現状のレイ扇（`NumTraces`本）を踏襲し、各レイが個別にヒット→透過→継続（多ホップ）する。コーンが斜めにガラスへ当たる場合、ガラスに当たったレイだけが色変化した継続レイになり、当たらないレイはそのまま。
- **レイ本数**: 近似精度と負荷のトレードオフを取る外部化パラメータ。透過境界付近の色の切り替わりの滑らかさを本数で調整する。

---

## 見せ方・UI

-

---

## 作り方の方針

-

---

## 未決・相談したいこと

- 反射・屈折の伝播ルール: 本仕様のスコープ外（論点Q＝ステージ1は反射不採用）。将来ステージで別途仕様化する。

