---
tags: [Colours, 仕様, 実装]
---
> **このファイルに書くもの**: 技術的な作り方の概要（どう実現するか）と実装ログへのリンク。
> **書かないもの**: ゲームロジック・ルール（→ `010.020.仕様`）、演出の要件（→ `010.030.演出・UI`）、アセット名・具体的なソースファイル名（→ 実装ログへ）。

# 実装: 光

## 実装方針

- 内部状態は CMY の3値で管理する。表示時のみ RGB 変換（R=1-C, G=1-M, B=1-Y）を行う
- しきい値はバランス調整パラメータとして外部化する

> 未決: UE5 での型方針（USTRUCT/UENUM、Blueprint 公開要否、FLinearColor との変換）→ 問題点メモ D-1（`010.999.問題点` 2026-06-26 削除済み）
> 未決: CS-1（型定義）と CS-3（照合判定）の設計を同時に検討する必要がある → 問題点メモ C-1 参照

## 実装ログ

> 未決: 実装ログへのリンクをここに記載する
