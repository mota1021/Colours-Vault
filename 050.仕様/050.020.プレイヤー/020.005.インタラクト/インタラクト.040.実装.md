---
tags: [Colours, 仕様, 実装]
---
> **このファイルに書くもの**: 技術的な作り方の概要（どう実現するか）と実装ログへのリンク。
> **書かないもの**: ゲームロジック・ルール（→ `インタラクト.020.仕様`）、演出の要件（→ `インタラクト.030.演出・UI`）、アセット名・具体的なソースファイル名（→ 実装ログへ）。

# 実装: インタラクト

## 実装方針

### インタラクト可能オブジェクト

- オブジェクト側は `IInteractable` インターフェースを実装する。
- 固有処理の実体はインタラクトコンポーネントが持ち、`IInteractable` の実装はそのコンポーネントへ委譲する。
- 個別のインタラクトアクション（拾う等）は、インタラクトコンポーネントの派生クラスとして実装する。

### 有効化・無効化

- コリジョン内にプレイヤーキャラクターが入ったら対象をインタラクト可能にし、抜けたら解除する。
- インタラクト可能な間は、実行対象となるオブジェクトを保持する。

### 選択ロジック

- 複数候補がある場合、PCの前方方向と各候補への方向の角度差を比較し、最小の候補を選択対象にする。
- カメラの向きは選択判定に使用しない。
- 選択対象が変わったときは、輪郭強調の対象も切り替える。

### 実行とクールタイム

- インタラクト実行時は、選択対象の `IInteractable` を通じて固有処理へ委譲する。
- クールタイム制御は共通の仕組みを利用する。
- クールタイムの適用条件と時間は各インタラクトコンポーネント側で管理する。

## 実装ログ

[実装ログへのリンク]
