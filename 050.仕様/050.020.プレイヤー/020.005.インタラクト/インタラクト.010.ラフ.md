---
tags: [Colours, 仕様, ラフ]
---
# 仕様ラフ: [仕様名をここに書く]

> **使い方**: このフォルダ全体を `[仕様番号].[仕様名]/` にコピーして使う。
> **このファイルだけ**を編集する。境界の判定（何を仕様に書くか/演出に書くか等）は生成時にAIが行うので、ここでは自由に混在して書いてよい。
> 変更・追記したら「このラフから仕様を更新して」と依頼 → AI が `インタラクト.020.仕様` / `インタラクト.030.演出・UI` / `インタラクト.040.実装` を生成・更新する。
> ラフは出力後も保持する（次回変更のための入力）。

---

## やりたいこと・目的

- プレイヤーキャラクターがオブジェクトに対して一定の距離に近づくと、オブジェクトにインタラクトできる
- インタラクトすると、オブジェクト固有の処理が実行される

---

## ルール・挙動

[条件・状態遷移・数値・判定ルールなど。画面表現や実装の話が混ざっていてもよい]

- コリジョン内にプレイヤーキャラクターが入るとインタラクトが可能になる
- 複数オブジェクトに対するインタラクトが可能な状況では、カメラの注視点により近い方のオブジェクトのインタラクトが優先される
	- 要検討、キャラクターの前方基準の方が直感的な場合もある
- インタラクトを実行すると、一定時間のクールタイムが発生する
	- 外部指定パラメータ
- 

---

## 見せ方・UI

[プレイヤーが見る・聞く・感じる要素。エフェクト・SE・HUD・アイコンなど]

- インタラクト可能なオブジェクトの輪郭が強調表示される

---

## 作り方の方針

[技術的にどう作るか。UE5の構造や他システムとの関係など。概要でよい]

- コリジョンに入ったら有効、抜けたら無効
- プレイヤーキャラクター側の持つ機能？
- インタラクト可能時はインタラクトするオブジェクトを保持しておく
- オブジェクト側は `IInteractable` インターフェース（外部への「インタラクト可能」の宣言・窓口）を実装する。固有処理の実体はインタラクトコンポーネントが持ち、`IInteractable` の実装はそのコンポーネントへ委譲する【2026-07-13決定】
- 個別のインタラクトアクション（拾う等）はインタラクトコンポーネントの派生クラスとして作る【2026-07-13決定】

---

## 未決・相談したいこと

[まだ決めきれてないこと、Claudeに相談したいことを箇条書きで]

-


## 打ち合わせログ

### 2026-09-20

#### 論点: 複数インタラクト候補の優先基準
- AI: 複数のインタラクト候補がある場合の優先判定を、カメラ注視先ではなくPC（プレイヤーキャラクター）の向き基準へ変更する案を確認。候補のうち、PCの前方方向との角度差が最小の対象を優先し、カメラだけを動かしても優先対象は変わらないものとする。
- User: インタラクトの優先度を、カメラ注視先基準からPCの向き基準へ変更する。


#### 論点: インタラクトのクールタイム所有
- AI: 既存の承認済み「モノを置く」実行計画・決定ログを確認した結果、クールタイムは「各インタラクトコンポーネント側で判定し、共通のクールタイム機構を利用する」という既存決定に整理できる。インタラクト基盤全体を一律に停止する共通クールタイムではなく、各アクション／オブジェクトが適用条件と時間を持つものとする。
- User: 既存資料間の不一致を解消する。


#### 論点: 選択対象の強調表示数
- AI: 複数のインタラクト候補が存在する場合でも、輪郭強調するのは現在の選択対象1つだけとする案を提示。
- User: 「複数候補があっても強調表示は1つのみ」を採用する。
