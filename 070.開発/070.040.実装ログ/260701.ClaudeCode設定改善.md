着手日: 260701
完了日: 260701（E:側に一部残課題あり）
完了状況: Vault管理側は完了／E:側は主要部完了・ユーザー操作待ち2件

仕様リンク: [[]]
関連コード: `.claude/`, `CLAUDE.md`, `ARCHITECTURE.md`, `docs/`（E:側）／`個人計画/`, `.claude/skills/`（Vault側）

---

## ClaudeCode 設定改善（性能向上・トークン節約）

計画ファイル: `C:\Users\mota\.claude\plans\colours-claudecode-claude-md-sprightly-bonbon.md`

### 実施内容

非コード作業（設定・ドキュメント）。再現に必要な手順・結果を記す。

1. **CLAUDE.md スリム化**（69行→48行）。毎セッション常時ロードの重複を削減。
   - 実装ログ書式の詳細・ドキュメント分割基準・Vault役割分担の詳細を削除し、正本（テンプレ・`ARCHITECTURE.md`）へのポインタに置換。
   - 新セクション「§5 作業モードの選択」を追加（小タスク=mainインライン / 複雑タスク=エージェント使用の判断基準）。
2. **DebuggingPlaybook 分割**。原則・技法は `docs/DebuggingPlaybook.md` に残し、Step7 の切り分けフロー図（付録）を新設 `docs/DebuggingCases.md` へ移設。着手時は原則側のみ Read。
3. **サブエージェント統合**（8→4）。
   - 新設 `.claude/agents/colours-planner.md`（要件整理＋調査起点＋設計＋計画検証を1本化）。
   - `requirements-analyzer` / `implementation-planner` / `implementation-validator` / `code-explorer` / `documentation-maintainer` を `.claude/agents-archived/` へ移動（削除ではなく移動＝可逆。`_README.md` に統合先を記載）。
   - アクティブは colours-planner / ue-implementer / test-runner / code-reviewer の4本。
   - モデルはコスト最適化: 読取系（planner/reviewer/test-runner）= Haiku 4.5、実装（ue-implementer）= Opus 4.8。planner/reviewer は品質が要る場面で `model: claude-sonnet-5` に1行変更で昇格可（各ファイルに注記済み）。
   - `ue-implementer.md` の `@documentation-maintainer` 委任参照を「main がインライン処理」に修正。
4. **実装ログフック補修** `.claude/hooks/check-impl-log.ps1` / `.claude/settings.json`。
   - コマンド判定正規表現を `dv\s+commit` → `dv\s+commit|commit\.bat` に拡張。
   - PreToolUse の matcher を `Bash` → `Bash|PowerShell` に拡張。commit.bat／PowerShell ツール経由のコミットでも発火するようになった（UE Submit Content は ClaudeCode 外のため対象外）。
5. **メモリ統合**（`C:\Users\mota\.claude\projects\E--UEProjects-Colours\memory\`）。
   - `project_content_git` を `feedback_git_workflow` へ統合し削除。
   - `project_cpp_structure` をクラス索引ポインタ（正本=ARCHITECTURE.md）に縮約。
   - `reference_debugging_playbook` に Cases.md 分離を反映。`MEMORY.md` 索引を更新。

### 残課題・TODO

- **許可リスト拡張**（未完・要ユーザー操作）: `.claude/settings.local.json` への `dv status` 等の追加は自動モードに拒否された（権限付与はユーザー明示承認が必要）。対話セッションで `/fewer-permission-prompts` を実行するか手動で allow に追加する。
- **MCP 棚卸し**（未完・要ユーザー操作）: `taskmaster-ai`（CLIでsampling不可）・`unreal-mcp`（PIEクラッシュ歴）の要否を対話セッションの `/mcp` で確認し、未使用なら無効化。

### 動作確認結果

- `.claude/settings.json` / `settings.local.json` の JSON 妥当性 OK（ConvertFrom-Json）。
- アクティブエージェント4本・モデル割当を確認。
- フック正規表現・matcher を確認。
- CLAUDE.md 全9セクション健在をReadで確認（削除項目はポインタで保全）。
- 未検証: フックのブロック/通過の実挙動（次回コミット時に確認）。

### 知見

- ClaudeCode の自動モード分類器は「自己設定変更」（エージェント定義の削除・パーミッション付与）を、プラン承認だけでは許可しない。エージェント整理は削除でなく `.claude/` 外への **Move-Item 退避**にすると憲法（無断削除禁止）とも整合して通る。
- 実装ログフックは matcher が `Bash` のみ＆`dv commit` 文字列一致依存だと、実運用の `commit.bat`／PowerShell ツール経由で素通りしていた。matcher とコマンド正規表現の両方を広げないと実効しない。

---

## ClaudeCode 設定改善 — Vault管理側（性能向上・トークン節約）

計画ファイル: `C:\Users\mota\.claude\plans\colours-claudecode-claude-md-jaunty-blossom.md`

### 実施内容

非コード作業（Vault内のスキル定義・運用docs整理）。上記エントリ（E:\UEProjects\Colours\ 側）に続き、Vault管理側（`D:\document\ObsidianVault`）を対象に実施。対話セッションでユーザーに「スコープはVault側のみ」「対象4項目すべて含める」を確認済み。

1. **運用ガイド.md 整理**（`個人計画/運用ガイド.md`、137行→104行）。
   - 二重採番だった `## 7.`（タスクの入口／ファイル/フォルダ規約）を修正し、後者を `## 8.` に採番し直し。
   - §4「セッション末の手順」を `/colours-end` スキルへのポインタ＋要点箇条書きに圧縮（実行系正本はスキル側）。
   - §5「今日やる事の選定ロジック」は `colours-todo`/`colours-end` が「§5参照」と明示参照する正本のため変更なし。
2. **サブエージェント計画docs刷新**（旧8体→現行4体。正本 = `E:\UEProjects\Colours\.claude\agents\`、本日別途完了済み）。
   - `個人計画/サブエージェント計画/サブエージェント.md`: 旧5体（requirements-analyzer/code-explorer/implementation-planner/implementation-validator/documentation-maintainer）の個別詳細ブロックを削除し、現行4体（colours-planner/ue-implementer/test-runner/code-reviewer）の概要＋E:側正本ファイルへのポインタに刷新。
   - `計画セッションテンプレート.md`: ①要件整理/②調査/③設計の3ステップを `@colours-planner` 1ステップへ統合。
   - `実装セッションテンプレート.md`: `@implementation-validator`（計画レビュー）を削除（`colours-planner` の計画検証が吸収）、`@documentation-maintainer`（ドキュメント更新）を「main インライン実行」に置換。②実装/③テスト/④レビューは維持。
3. **スキル定義の整理**（`.claude/skills/`）。
   - **命名統一**: `spec-from-rough` → `colours-spec-from-rough`、`ticket-inbox` → `colours-ticket-inbox`（`git mv` で履歴保持。frontmatter `name:`・`description` 内の呼び出し別名も更新）。
   - **参照更新**: `創作/ゲーム/CLAUDE.md`、`AI/Instructions/スキル一覧.md`、`AI/スキル化候補リスト.md`（テーブル行＋実装済みスキル一覧のリンクパス）。
   - **重複削除**: `game-{start,end,todo,review,nippou,spec-from-rough,ticket-inbox}` 7本を削除。各 `SKILL.md` の frontmatter `name:` が canonical スキル名を指すコピーで、Vault内から参照ゼロ（skill index 未掲載）であることを事前にgrepで確認。憲法の「削除は別コミット・概要を残す」に従い、この7本の削除は他の変更と別コミットにする。

### 残課題・TODO

- なし（本計画のスコープ内はすべて完了）。E:\UEProjects\Colours\ 側の残課題（許可リスト拡張・MCP棚卸し）は上記エントリを参照（本エントリのスコープ外、ユーザーがスコープ確認で明示的に除外）。

### 動作確認結果

- 新セッションの利用可能スキル一覧で Colours 系が `colours-{start,end,todo,review,nippou,spec-from-rough,ticket-inbox}` の7本になり、`game-*` が消滅していることを確認（Skill tool の利用可能スキル一覧に新名2本が反映されたことも確認）。
- `git grep` で `game-(start|end|todo|review|nippou|spec-from-rough|ticket-inbox)` および無印 `spec-from-rough`/`ticket-inbox` の残存参照がゼロであることを確認（archive除外なしでも0件）。
- `運用ガイド.md` の `##` 見出しが 1〜8 で重複なく、§5選定ロジックが健在であることをReadで確認。
- 計画docs（サブエージェント.md・両テンプレート）に旧エージェント名への実行形式参照（`Agent: @xxx`）が残っていないことを確認（「旧〇〇は統合/廃止」という移行注記としての言及のみ残存、意図的に保持）。

### 知見

- スキルディレクトリの重複はfrontmatterの `name:` を見れば一発で判定できる（ディレクトリ名でなく `name:` が実体の識別子）。`game-end/SKILL.md` の `name: colours-end` のように、コピー元の名前がそのまま残っているケースは「意図せぬ複製」の強いシグナル。
- E:\UEProjects\Colours\ 側とVault側は別々のClaudeCode設定（別CLAUDE.md・別エージェント定義）だが、docsの陳腐化は連動する。E:側のエージェント構成を変えたら、Vault側の計画テンプレ（個人計画/サブエージェント計画/）も同時に点検しないと「正本と参照ドキュメントの不一致」が発生する。
