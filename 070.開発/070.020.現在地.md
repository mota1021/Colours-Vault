> ⚠️ **これは読み込み最適化キャッシュです。矛盾がある場合は各詳細ファイルが正。**
> 最終更新: 2026-09-21

# Colours — 現在地スナップショット

## プロジェクト

| 項目 | 内容 |
|---|---|
| コンセプト | 光と色で解く3Dアクションパズル |
| 技術 | Unreal Engine 5.8.0 / C++ |
| 現在のマイルストーン | M2 [[縦スライス_MVP|縦スライス（MVP）]] |
| 当面のゴール | [[縦スライス_MVP]]を1部屋完成させる |
| 次のゴール | ステージ1を完成させる |
| 北極星 | [[縦スライス_MVP]] |
| タスク正本 | Jira |
| 識別 | 意味名を人間向けに使用し、一意参照が必要な時だけ Jiraキーを使う |

## 現在の主対象

**携帯ライトの拾得・設置**

- M2 の次のプレイヤーループとして、携帯ライトを拾い、壁へ向けて床に置ける状態を成立させる。
- 設置後も点灯を維持し、置いた向きを保って照射し続けることが必要。

### 2026-09-21 進行中

- インタラクト候補検出は、DynamicMesh のコーン + Complex Collision 方式から、BPで配置する Shape Component 方式へ変更する方針に決定。現在の判定形状は前方 Box。
- 複数候補の優先判定は従来どおりPCからの距離を基準とし、ヒステリシスも維持する。
- `UColoursInteractionComponent` 自体も Character Blueprint 側で Add Component する。C++ Character は生成しない。
- `InteractionComponent` は Scene Component とし、直下の `UShapeComponent` 派生を判定範囲として自動検出する。Box / Sphere / Capsule 等へBP上で差し替え可能。
- 判定形状のサイズ・相対位置・Collision 設定はBP上の Shape Component で直接調整し、コードや DataAsset の調整値としては持たない。
- UEColours PR #12「インタラクト候補検出を前方Box方式へ変更」を作成済み。ローカルビルド・PIE確認待ち。

### 2026-09-20 終了地点

- インタラクト仕様を更新済み。
  - プレイヤー追従のコーン型判定範囲内を候補とする。
  - 候補間の優先判定は距離基準。
  - 選択切替にはヒステリシスを使用し、閾値と境界条件を調整可能にする。
  - インタラクト入力は F キー。
- プレイヤー側インタラクト基盤を UEColours に実装済み。
  - 候補収集と選択ロジックを分離。
  - 選択ロジックは差し替え可能な構造。
  - `IColoursInteractable` の共通窓口を追加。
  - 選択対象1つだけへ強調通知を送る。
- プレイヤー設定は Project Settings ではなく DataAsset 階層へ変更済み。
  - PlayerCharacter DataAsset をプレイヤー設定の入口とする。
  - そのメンバーとして PlayerInteract DataAsset を参照する。
  - コーン範囲、ヒステリシス閾値、境界条件は PlayerInteract DataAsset に保持する。
- UEColours PR #5「インタラクト基盤: コーン候補検出と距離ヒステリシス」は master へマージ済み。
  - merge commit: `b8e0d0e601a4cd3784e41141da266a649591af4b`
- 現段階では、Fキーで共通 `Interact` を呼び出す基盤まで。
  - オブジェクト側の共通インタラクトコンポーネントは未実装。
  - 「拾う」固有コンポーネントは未実装。
  - 携帯ライトを実際に拾って所持状態へ移す処理は未実装。

### 2026-09-21 再開地点

1. **前方 Box 方式のインタラクト候補検出をローカルへ反映してビルドする。**
2. `BP_ColoursPlayerCharacter` に `InteractionComponent` を Add Component し、その直下に Box Collision を追加する。Box Extent / Relative Location / Collision 設定は Box 側で直接調整する。
3. PIEで `BP_TestInteractable` が範囲内で候補になり、複数候補では近い対象が選ばれることを確認する。
4. 確認後、診断作業を終了して携帯ライトの「拾う」接続へ戻る。

**2026-09-21 の次の一手:** UEColours PR #12 をローカルでビルドし、BPに `InteractionComponent` と子の Box Collision を追加してPIE確認する。

## 消える壁の色ゲート統合

- UEColours PR #4 は master へマージ済み。
- 2026-09-20 にローカル UE5.8.0 Development Editor / Win64 ビルド成功。
- PIEで、白+白、赤+赤、青+赤、光を外した復活、異色時の断面非表示を確認済み。
- 色ゲート統合作業は完了。

## 光の多ホップ伝播

- UEColours PR #3 は 2026-09-15 に master へマージ済み。
- ただしPR上ではローカルビルド・PIE確認未実施のままマージされている。
- 縦スライス必須ではないため、追加検証・調整は縦スライス完成後へ後回しとする。

## 参照

- [[創作/ゲーム/Colours/shared/060.level_design/060.000.プロトタイプ/縦スライス_MVP|縦スライス_MVP]]
- [[創作/ゲーム/Colours/shared/050.仕様/050.020.プレイヤー/020.005.インタラクト/インタラクト.040.実装|インタラクト — 実装]]
- [[創作/ゲーム/Colours/shared/050.仕様/050.020.プレイヤー/020.010.モノを拾う/モノを拾う.040.実装|モノを拾う — 実装]]
- [[創作/ゲーム/Colours/shared/050.仕様/050.030.ギミック/030.020.消える壁/消える壁.040.実装|消える壁 — 実装]]
- [[創作/ゲーム/Colours/shared/070.開発/070.010.ロードマップ|ロードマップ]]
- [[創作/ゲーム/Colours/shared/900.AIエージェント用/非同期Task/光の多ホップ伝播/_全体図|光の多ホップ伝播 — 全体図]]
